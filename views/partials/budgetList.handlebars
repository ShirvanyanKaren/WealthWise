<select id="budget-dropdown" class="input-field">
  <option value="" disabled selected>Select a Budget</option>
  {{#each budgets}}
    <option value="{{id}}">{{budget_name}}</option>
  {{/each}}
</select>